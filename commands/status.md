---
description: Show inbox stats and current top 10.
---

Show a quick dashboard:

1. Count of ideas by status (Inbox / Researching / Validated / Building /
   Shipped / Killed) from `$PWD/inbox.md`.
2. Count of ideas with `research.md` present, count with score (walk
   `$PWD/ideas/*/`).
3. Current top 5 from `$PWD/top-ideas.md` (link only, do not paste full
   file).
4. Any ideas in `Researching` status without `research.md` (the queue).
5. Any ideas with score >= 80 still in `Researching` (action: review for
   promotion to Validated).

Keep output to a single concise screen. No fluff.
