---
description: Initialize a new Idea Vault project in the current directory.
---

You are running the **init** workflow. This scaffolds project files into
the current working directory so the user can start using the vault.

Read the init skill at `${CLAUDE_PLUGIN_ROOT}/skills/init/SKILL.md` and
follow it.

Final output: confirm what files were created, list any next steps.
